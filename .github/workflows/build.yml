name: Qt CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Install Qt
      uses: jurplel/install-qt-action@v2
      with:
          modules: qtwebengine
    - name: Install libusb
      run: |
          libusb-dev
    - name: qmake
      run: qmake qserial.pro
    - name: make
      run: make
